<Window x:Class="DesktopRestorer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DesktopRestorer"
        mc:Ignorable="d"
        Title="桌面恢复工具" Height="500" Width="600" ResizeMode="CanMinimize" WindowStartupLocation="CenterScreen">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- 源文件夹选择 -->
        <GroupBox Grid.Row="0" Header="源文件夹（文件夹A）" Margin="0,5,0,5" Padding="5">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox x:Name="SourceFolderTextBox" Grid.Column="0" Margin="0,0,5,0" IsReadOnly="True"/>
                <Button x:Name="BrowseSourceButton" Grid.Column="1" Content="浏览..." Width="80" Click="BrowseSourceButton_Click"/>
            </Grid>
        </GroupBox>
        
        <!-- 目标文件夹（桌面）显示 -->
        <GroupBox Grid.Row="1" Header="目标文件夹（桌面）" Margin="0,5,0,5" Padding="5">
            <TextBox x:Name="DesktopFolderTextBox" IsReadOnly="True"/>
        </GroupBox>
        
        <!-- 开机自启动选项 -->
        <CheckBox x:Name="AutoStartCheckBox" Grid.Row="2" Content="开机时自动启动并恢复桌面" Margin="5,10,0,5" Checked="AutoStartCheckBox_Checked" Unchecked="AutoStartCheckBox_Unchecked"/>
        
        <!-- 自动备份设置 -->
        <GroupBox Grid.Row="3" Header="自动备份设置" Margin="0,5,0,5" Padding="5">
            <StackPanel>
                <CheckBox x:Name="AutoBackupCheckBox" Content="启用自动备份" Margin="0,5,0,0" Checked="AutoBackupCheckBox_Checked" Unchecked="AutoBackupCheckBox_Unchecked"/>
                <Grid Margin="0,5,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="备份间隔(分钟):" VerticalAlignment="Center"/>
                    <ComboBox x:Name="BackupIntervalComboBox" Grid.Column="1" Margin="5,0,0,0" SelectedIndex="0" Width="120" HorizontalAlignment="Left">
                        <ComboBoxItem Content="30"/>
                        <ComboBoxItem Content="60"/>
                        <ComboBoxItem Content="120"/>
                        <ComboBoxItem Content="240"/>
                        <ComboBoxItem Content="480"/>
                    </ComboBox>
                </Grid>
            </StackPanel>
        </GroupBox>
        
        <!-- 操作按钮 -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" Margin="0,10,0,10" HorizontalAlignment="Left">
            <Button x:Name="BackupNowButton" Content="立即备份" Margin="0,0,10,0" Padding="5" Width="120" Click="BackupNowButton_Click"/>
            <Button x:Name="RestoreNowButton" Content="立即恢复桌面" Padding="5" Width="120" Click="RestoreNowButton_Click"/>
        </StackPanel>
        
        <!-- 日志显示区域 -->
        <GroupBox Grid.Row="5" Header="操作日志" Margin="0,5,0,5" Padding="5">
            <ScrollViewer VerticalScrollBarVisibility="Auto" Height="150">
                <TextBox x:Name="LogTextBox" IsReadOnly="True" TextWrapping="Wrap" VerticalAlignment="Stretch" Background="#FAFAFA"/>
            </ScrollViewer>
        </GroupBox>
        
        <!-- 状态栏 -->
        <StatusBar Grid.Row="6" Height="25">
            <StatusBarItem>
                <TextBlock x:Name="StatusTextBlock" Text="就绪"/>
            </StatusBarItem>
            <StatusBarItem>
                <ProgressBar x:Name="BackupProgressBar" Width="200" Height="16" Minimum="0" Maximum="100" Value="0" Margin="10,0,0,0"/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>
